<div class="container-fluid">

<% if user_signed_in? %>
<div ng-view></div>
<% else %>
<div class="hero-unit">
    <h1 class="text-center">gearstack.</h1>
    <p class="text-center">welcome to gearstack, the gear storage app.</p>
    <p class="text-center"><%= link_to "sign in", new_user_session_path %> or <%= link_to "sign up", new_user_registration_path %>
</div>
<% end %>

    <p><%= notice %></p>
    <p><%= alert %></p>


<% if user_signed_in? %>
  <p><%= link_to "sign out", destroy_user_session_path, :method => :delete %></p>
<% end %>

</div> <!-- /container-fluid -->